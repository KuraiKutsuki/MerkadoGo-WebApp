# Progress Log

## Session: 2026-08-31
- Initialized planning files (`task_plan.md`, `tasks/todo.md`, `findings.md`, `progress.md`) according to `planning-with-files` and `planning-and-task-breakdown` skills.
- Established Phase 1-5 detailed checklists.
- Created `README.md` containing project overview, architectural specifications, and full tech stack breakdown.
- Completed Task 1.1: Initialized Vite Vanilla JS project, verified dependencies and build bundle.
- Completed Task 1.2: Implemented full Civic Craft & Flat Utility CSS design tokens and JS palette mapping.
- Executed `$impeccable init`: Generated `PRODUCT.md` and configured `.impeccable/config.json`.
- Completed Task 1.3: Built complete full-screen layout shell in `index.html` and `src/style.css`, verified with Impeccable detector and Vite build. Phase 1 complete.
- Completed Task 2.1: Injected `LigaoCity_PublicMarket_Map.svg` into DOM `#map-viewport` with `#map-transform-layer` and `#route-layer`, indexing 463 market stall shapes.
- Completed Task 2.2: Built `MapControls` in `src/mapControls.js` implementing multi-touch pinch-to-zoom, pointer dragging, zoom buttons, reset view, and auto-centering on market stalls.
- Fixed 3 map rendering issues:
  1. Centered initial ViewBox directly onto market stalls (`x: 3360, y: 3390, width: 2800`) to eliminate excessive zoomed-out space.
  2. Implemented 1:1 direct touch coordinate tracking for instant, fluid swiping in all directions without resistance.
  3. Removed `vector-effect: non-scaling-stroke` and forced CSS stroke overrides to preserve authentic 2px vector outlines from the SVG.
  4. Added dynamic Pan & Zoom boundary constraints (`centerBounds`) and aspect-ratio-aware max zoom limits to eliminate bottom/top empty space on portrait screens.
  5. Added prominent Recenter / Target button to the floating controls stack to instantly recenter on the market stalls.
  6. Reorganized all runtime assets into a dedicated `public/` directory (`public/map/`, `public/assets/`, `public/data/`), keeping `documents/` strictly for documentation and guides.
  7. Fixed recenter button flexbox box-sizing centering (100% width/height) and added robust pointer-down active micro-animation feedback for mobile and desktop.
  8. Added 2-finger multi-touch rotation gesture with rotation-aware panning, auto-snapping to North (0°), and live compass orientation on the recenter button.
  9. Implemented Google Maps grade kinetic physics engine: momentum inertia gliding (flick/fling pan), angular momentum rotation, and double-tap to zoom with cubic ease-out transitions.
  10. Fixed rotation coordinate transformation sign in `src/mapControls.js` so swiping left/right/up/down follows the exact visual finger direction at any rotation angle (90°, 180°, 270°).
  11. Removed incorrect rotation correction from ViewBox panning in `src/mapControls.js`. The old code applied a rotation matrix to screen-space deltas, which inverted swipe direction at non-zero rotation angles (e.g., swiping up at 90° moved content left). Fix: ViewBox panning operates in SVG root coordinate space — screen deltas map directly to ViewBox deltas with no rotation compensation needed, since the `<g>` transform layer rotation is independent.
- Completed Task 2.3: Initial Styling & Hit Testing:
  - Applied default Unassigned palette (`fill: #E2E8F0`, `stroke: #94A3B8`, `stroke-width: 2px`) to all 463 indexed stall elements across the SVG DOM.
  - Implemented delegated hit-testing event listener (`setupStallHitTesting`) on `#market-svg` in `src/mapRenderer.js`.
  - Added visual selection state (`.stall--selected` with high-contrast ink outline) and single-stall selection management.
  - Verified click hit-testing, selection toggle, and console logging via automated browser test suite. Phase 2 (Map Rendering & Interactivity) is 100% complete.

## Session: 2026-09-02
- Completed mandatory source-document review: all 7 Dev Vault docs in `d:/Projects/Dev Vault/02 - Projects/MerkadoGO Map/`, `documents/MerkadoGo_WebApp_Master_Context.md` (367 lines), and the full `database_schema/` bundle (`firestore.rules`, `schema_types.ts`, `stall_model.dart`, `market_categories.dart`, JSON assets).
- Verified datasets first-hand: 134/134 vendor records have `stall_id`; 14 entrances; 17 search directory slugs nested under top-level `categories` object (not flat — dataStore must handle); category slug bridging via `primaryCategoryName` matching (`market_categories.dart`).
- Amended `tasks/todo.md`: added **Task 3.4 (Live Firestore Stall Sync)** to Phase 3, matching the vault `Active Status & Roadmap` 4-task definition, with acceptance/verification derived from Master Context §4.1 and Database Schema §6. Added live-sync item to Phase 3 checkpoint.
- Context review complete; awaiting explicit user approval to begin Task 3.1 (Load Static Assets → `src/dataStore.js`).
- Reviewed and applied 3 external (GLM) pre-flight findings, all verified correct first-hand:
  1. Missing dependency: installed `firebase@^12.18.0` (runtime dep; needed for Task 3.4 `stallSync.js`). Vite build re-verified clean after install.
  2. Static asset source: `public/data/vendor_notes.json` was the legacy export (0/134 `stall_id`); replaced with enhanced `database_schema/json_assets/vendor_notes.json` (134/134 `stall_id`, strict superset — same records, only `stall_id` added).
  3. Heading alignment: `tasks/todo.md` Phase 3 heading updated from "(Local JSON Mock)" to "(Static JSON Fallback + Live Firestore Sync)" to match `task_plan.md`; Task 3.4 Files list now notes the `firebase` package.json requirement.
- Completed Task 3.1: Load Static Assets (`src/dataStore.js`):
  - Built pure data layer fetching `/data/map_nodes.json`, `stall_nodes.json`, `market_entry_points.json`, `subcategory_search_directory.json` in parallel, with per-dataset validators (Master Context §3.1–§3.5): graph edge symmetry/dangling warnings at parse time, stall node string-vs-array shape check, entrance→graph node reference check, directory `categories` nesting check.
  - Exposed `loadStaticData()` (cached), getters, and `getStallNodeIds()` normalizing both legal stall_nodes shapes into string arrays with index-0 primary preserved.
  - Wired into `src/main.js` (appState.staticData + window.merkadoApp debug handle).
  - Verified in live browser (Vite dev @ localhost:5173): all 4 datasets in memory (116 nodes / 231 stall mappings / 14 entrances / 17 categories), 0 validation warnings, cache reuse confirmed (same reference, no refetch), normalization correct for `id_3` (array) and `id_9` (string), unknown ID → `[]`. `npm run build` clean.
  - CORRECTION to Phase 2 record: the earlier "463 indexed stall elements" figure was a raw pre-dedup querySelectorAll count. True unique stall shapes = 231 (134 `id_*` vendor stalls + 97 `slot_*` vacant slots), a perfect 1:1 match with `stall_nodes.json`'s 231 keys.
- Completed Task 3.2: Load & Apply Vendor Data:
  - `src/dataStore.js`: added `normalizeStallRecord()` (canonical vendor shape mirroring the mobile model's defensive fallbacks; all IDs string-modeled — split records `id_41.1`/`id_41.2`/`id_257.2` safe; `map_color_hex` deliberately dropped, color comes only from `primary_category` → ZONE_PALETTE) + `loadVendorData()`/`getVendorData()`/`getVendorByStallId()` with duplicate stall_id detection.
  - `src/mapRenderer.js`: added `applyVendorToStall()` (single-stall applier, reusable by Task 3.4 live sync) and `applyVendorColors()` (bulk with stats). Unknown stall IDs warn-and-skip (Master Context §10).
  - `src/main.js`: static data + vendor data fetched in parallel; coloring applied after SVG index.
  - Verified in live browser: 134/134 vendor polygons carry the exact expected ZONE_PALETTE fill+outline (0 mismatches), 97 vacant slots remain Unassigned (231 = 134 + 97), split stalls colored correctly (`id_41.1` Meat #E57373, `id_257.2` Rice #E5A93C), defensive bogus-ID path warns without throwing. Visual screenshot confirms semantically coherent sections (Rice gold, Meat salmon, Fish blue, Eateries orange). `npm run build` clean.
- Completed Task 3.3: Stall Details UI Card (under `impeccable` skill governance; Operate mode, local extension of established Civic Craft world):
  - New `src/uiController.js`: `initStallDetailCard()` populates vendor name, category badge (with flat ZONE_PALETTE swatch dot tying card to polygon color), "STALL # • SECTION" tag (stall_number display-only, unparsed), and address. Vacant `slot_*` clicks keep the card hidden (Master Context §3.2 clickable-but-unlabeled). Dismissal paths: close button, Escape, tap-away on empty map (all also clear `.stall--selected`).
  - `index.html`: card switched from inline `display:none` to `hidden` attribute; Route button set `disabled` until Phase 4 wires routing (honest state).
  - `style.css`: `[hidden]` display guard (author flex would override UA rule), 160ms ease-out card entrance keyframe (the page's one authored motion, `prefers-reduced-motion` respected), `.badge-zone-dot` flat swatch, `.btn:disabled` flat state.
  - HIT-TESTING FIX: discovered 242 decorative Figma-export `rect[id^="Rectangle"]` marks overlaying stalls as siblings (0 inside stall groups) — taps landing on them missed the stall. mapRenderer now sets `pointer-events: none` on all of them at injection, restoring reliable delegated hit-testing (Master Context §4).
  - Verified in live browser (7-test matrix): open id_1 (2 CEE'S STORE / Rice & Grains / STALL #15 • BUILDING II + gold dot rgb(229,169,60)), close+selection-clear, switch to id_154 (MARILOU M GUANZON STORE / Ingredients / STALL #36 • BUILDING III), id_3 under decorative rect now opens (4E'S LLOBET MEATSHOP / Meat / STALL #1 • MEAT SECTION), vacant slot hides card, tap-away hides+clears, Escape hides. Build clean. Screenshots confirm flat Civic Craft rendering.
- Created `.env` (Vite format) from verified sources: `d:/Projects/MerkadoGo/.env` (web API key + Cloudinary read first-hand, values match vault records) + Dev Vault "Database & Live Firestore Schema" web app-id block. Flutter `firebase_options.dart` carries only the Android block (no web appId there) — web values taken from the vault's verified record; all cross-checkable values match. Out-of-scope keys (GEMINI_API_KEY, GOOGLE_MAPS_API_KEY) deliberately excluded — chatbot/GPS are Android-only. Dev server verified reachable on Wi-Fi adapter 192.168.254.105:5173 for phone viewing.
- Bugfix round (mobile phone viewing via LAN URL, reported by user):
  1. **White padding around map when zooming**: root cause — transparent artwork margins (canvas edges / removed background rect) showed the off-white `--color-canvas` page background. Fix: oversized ground `<rect id="map-ground">` (pathway gray, INFRA_PALETTE.pathway.fill) inserted as first child of `#map-transform-layer` in `mapRenderer.js`, plus `#map-viewport` background switched to new token `--color-map-ground: #E0E0E0` (canonical INFRA pathway color) so any transient letterbox also reads as map ground. Verified: max zoom-out shows uniform ground, zero white, on phone + desktop viewports.
  2. **Stall detail card cut off at bottom (Route button clipped)**: root cause — `#app { height: 100vh }` sizes to mobile Chrome's URL-bar-HIDDEN viewport, pushing bottom-anchored UI behind the URL bar. Fix: `height: 100dvh` fallback chain. Verified at 412x915: card bottom 899px vs 915px viewport, Route button fully visible.
  3. **Map control pills overlapped the open card** (z-index 20 vs card 15): added `#app.detail-open .map-controls-group { opacity: 0; pointer-events: none }` toggle driven by uiController (Google Maps-style yield), 160ms transition.
  - User's phone URL: http://192.168.254.105:5173 (Wi-Fi adapter; confirmed HTTP 200).
- User feedback round on bugfixes:
  1. **Ground color reverted** to the original warm off-white: user preferred `--color-canvas` (#F7F7F5) over the pathway-gray ground. SVG ground rect now fills APP_COLORS.canvas; `#map-viewport` background uses `var(--color-canvas)`. The uniform-ground *fix* (no mismatched white/canvas seams at zoom edges) is retained — just in the original color.
  2. **Map controls anchored back to the bottom-right corner**: a pre-existing mobile media query lifted the group 80px up (`bottom: max(80px, …)`) — a leftover workaround from the 100vh era. Now `bottom: calc(24px + env(safe-area-inset-bottom))` — verified 24px from bottom / 16px from right at 412x915.
  3. Controls still yield (fade) while the detail card is open — flagged to user for veto.
  - Build clean; verified phone-viewport screenshots (default view + card open).
- Completed Task 3.4: Live Firestore Stall Sync — **Phase 3 complete**:
  - `src/services/stallNormalizer.js`: canonical `normalizeStallDoc(docId, data)` per vault Database doc §4 — handles both static JSON + Firestore field names, string IDs, status/isOpen/hours/photos/tags. `dataStore.normalizeStallRecord()` now delegates to it (one normalization path for both sources).
  - `src/services/stallSync.js`: `initLiveStallSync(onStallUpdate, onError)` — dynamic-imports firebase/app + firebase/firestore (code-split; main bundle stays ~10kB gzip), subscribes `onSnapshot(collection(db,'stalls'))`, forwards normalized changes; config guard + full error path. `.env` VITE_FIREBASE_* consumed.
  - `src/dataStore.js`: `upsertVendorRecord()` / `removeVendorRecord()` — live changes flow into the same in-memory store the detail card reads, so the card always shows live truth.
  - `src/main.js`: `handleLiveStallChange(changeType, stall)` — added/modified → `applyVendorToStall()` recolor + store upsert; removed/closed/inactive → Unassigned reset + store removal; open detail card refreshes live if it's showing that stall; unknown SVG ids warn-and-skip.
  - Verification: (a) REAL Firestore probe → `permission-denied` (rules not deployed to live project) — app warned and continued on static fallback, proving the resilience path with a genuine error; (b) 6-test simulated pipeline through the production handler, all passing: recolor+live card refresh, closed→Unassigned+store clear, removed→reset, added-on-vacant-slot, bogus-id warn-no-crash, split-stall id_41.1; (c) build clean. Also hardened `buildStallTag` against missing fields (caught by the simulation).
  - **BLOCKER for true live sync**: deploy `database_schema/firebase/firestore.rules` to the `merkado-go` Firebase project (console or `firebase deploy --only firestore:rules`). Until then the app intentionally runs on static data.
- Completed Task 4.1: Build A* Graph (`src/pathfinder.js`) — Phase 4 started:
  - `buildPathfindingGraph(mapNodes)`: pure builder materializing `{ nodes, adjacency }` where `adjacency[nodeId] = [{ id, weight }]` with Euclidean SVG-unit weights (Master Context §7.1/§7.2). Dedupes duplicate neighbor entries (mobile reference hit this on id_39), excludes coordinate-less nodes and dangling neighbors with warnings, never mutates input.
  - `verifyPathfindingGraph(graph)`: pure — edge reciprocity (asymmetric edge list), connectivity via undirected flood fill (walkway semantics; one-way adjacency entries are data bugs reported separately, not physical one-way corridors), isolated nodes, isConnected, edgeCount.
  - `analyzeReachability(graph, requiredNodeIds)`: pure — every entrance gate + primary (index-0) stall snap node must exist and sit in the main walkway component.
  - `euclideanDistance()` exported as the shared metric (edge cost = heuristic basis → admissible & consistent per Master Context §7.2).
  - Wired into `main.js` after static data load; console verification logs added; graph exposed at `window.merkadoApp.pathGraph`.
  - Verified live in browser (dev server + in-page test suite against the real module): 116 nodes / 388 directed edges (194 corridors) / 0 asymmetric edges / 1 connected component / 0 isolated nodes; 245/245 routing-critical nodes reachable (14 entrances + 231 primary snaps), 0 missing, 0 unreachable; sample edge weight exactly matches independently computed Euclidean distance; purity confirmed (input not mutated, rebuild deterministic); 10/10 defensive malformed-input tests pass (missing coords, dangling neighbor, duplicate neighbor, empty/null input, asymmetry reporting, disjoint-component detection, reachability flags); `npm run build` clean, main bundle ~10.8kB gzip unchanged.
  - Dev Vault deep-read this session (companion mobile hub, Strict Execution Rules, UI-UX Design Rules, Zero-Assumption Protocol, Home MOC) + Flutter reference `pathfinding_service.dart` ingested — key Phase 4.2 facts recorded in findings.md (turn thresholds 18°/45°/135°/165°, 50-unit straight-collapse minimum, zone-name instruction flavor, id_39 dedupe).
- Completed Task 4.2: Implement A* Search & Turn Generation (`src/pathfinder.js` + new `src/turnGenerator.js`) — Phase 4 core logic done:
  - User decision: MIRROR the mobile reference values (18°/45°/135°/165° turn buckets, 50-unit straight-merge minimum, zone flavor text) for cross-platform behavioral parity.
  - `findPath(graph, startNodeId, goalNodeId)`: pure A* — binary min-heap open set with monotonic FIFO tie-breaking (deterministic), lazy deletion, Euclidean heuristic over materialized edge weights; `[]` on unknown/disconnected endpoints (warn, never throw); parameter order follows Master Context §7.2 (`findPath(graph, startId, goalId)`), the vault roadmap's `(start, goal, graph)` order noted as its summary shorthand.
  - `getPrimarySnapNode(value)`: pure index-0 snap resolver accepting both stall_nodes.json shapes (Master Context §3.2).
  - `generateDirections(path, nodes, {entranceDescription, destinationName})`: start → straight/turn steps → arrive; step distances sum exactly to path length; zone flavor "Turn right at the intersection towards Wet Market" from node prefixes; instruction texts mirror mobile ("Enter via X and head into the aisle" / "Arrive at Y on your pathway").
  - MOBILE QUIRK FIXED: the Flutter merge only fired when the previous step was already a straight step — impossible since start/turn steps are the only kinds added — so its 50-unit rule was dead code and straight distance was silently dropped. This port flushes the pending straight run both at turn interruptions and after the final turn.
  - Verified live in browser: **3234/3234 routes (14 entrances × 231 stalls)** valid — 0 empty, 0 wrong endpoints, 0 adjacency violations, 0 revisits; **40/40 sampled routes provably optimal** (A* cost exactly equals an independent in-test Dijkstra); **3210/3210 direction lists** structurally valid with the exact distance-sum invariant; 5011 straight steps emitted, all > 50 units (merge logic demonstrably live); turn kinds observed on real geometry: right / left / slight-left / slight-right; deterministic (identical rerun); 16-case boundary table for classifyTurn + 12 edge cases (same-node, unknown ids, disconnected fake graph, single-node path, empty/unknown path, snap resolver) all green; `npm run build` clean. Sample route logged: "Straight From Church" → id_3 = 12 nodes / ~2265 units / 5 steps.
- Distance display decision RESOLVED by user (before 4.3): meters deliberately omitted — directions are angle-based only; no distances displayed anywhere; summary badge = step count only; internal SVG-unit distances kept solely for the 50-unit merge rule.
- Completed Task 4.3: Route UI Overlay & Entrance Selector (`uiController.js`, `mapRenderer.js`, + `index.html`/`style.css`/`main.js` wiring, new pure `pathfinder.getPathCost()`) — **Phase 4 complete**:
  - Under `impeccable` governance (Operate mode, local extension of the established Civic Craft world — inherited tokens, flat rules, 48px targets; craft-floor loaded before editing).
  - `mapRenderer.drawRoute()/clearRoute()`: flat `<polyline id="active-route">` in `#route-layer` (above stalls, inside `#map-transform-layer` — pans/zooms/rotates with the map); styling from the existing `#route-layer polyline` CSS rule.
  - `uiController.initNavigationPanel()`: entrance `<select>` populated from the 14 gates (unique `entrance_id`s verified), route recomputed on every gate change, instruction list = step number + text (NO distances), "N Steps" summary, Clear Route resets to pre-route state while the panel stays open, X / Escape / tap-away close+clear. Nearest entrance preselected by shortest A* WALKING distance ( getPathCost over all 14 gates — beats straight-line heuristic because it respects corridors).
  - `main.js`: Route button (now ENABLED — it was still `disabled` from Task 3.3 and silently swallowed taps) opens the panel for the selected stall; card yields (one sheet at a time); destination stall keeps its selection outline; `#app.nav-open` extends the controls-yield CSS; viewport auto-frames the route bbox (fit-width from container aspect + 40% padding).
  - `index.html`: `hidden` attributes replace inline `display:none` (with matching `[hidden]` CSS guards — same author-flex gotcha as the stall card); `#route-distance` span + divider REMOVED per the no-distances decision.
  - **CRITICAL DISCOVERY — node→SVG coordinate calibration**: the first visual check showed a BLANK map — map_nodes.json coordinates live in a DIFFERENT frame than the SVG (all 116 nodes at negative x ∈ [-5724, -2293] vs stalls at x ∈ [2095, 4689]; Master Context §3.1's "line up 1:1 with the SVG viewBox" claim is FALSE for this dataset). Mirroring the companion mobile app's hand-calibrated constants (interactive_market_map.dart `_nodeOffsetX=7706.4/_nodeOffsetY=3163.3`, applied there as a pure translation for its RouteOverlayPainter and entrance markers), `mapRenderer.NODE_TO_SVG_OFFSET` now translates route geometry in `drawRoute()` and in the route-bounds framing. Routing math stays in node space (Euclidean metrics are translation-invariant — all 4.1/4.2 verification remains valid). Post-fix: route endpoint lands 78 units from id_3's center (the walkway edge where its snap node sits).
  - Verified: full functional matrix green (card→panel handoff, 14-option selector, entrance switch recomputes 3→5 steps, clear/escape/tap-away/stall-tap behaviors, vacant-slot `openForStall` rejected, controls yield to opacity 0, destination keeps outline, no distance markup anywhere); visual screenshots at desktop 1280×720 and phone 412×915 confirm the flat green route line running corridor-aligned to the outlined destination stall with the Civic Craft panel; `npm run build` clean.

- Completed Task 4.4: Exact Pathway Calibration & Animated Route Tracing (`src/mapRenderer.js`, `src/style.css`, `src/uiController.js`):
  - Measured and verified against all 112 vector node rects in `LigaoCity_PublicMarket_Map.svg`. Discovered the prior offset `{ x: 7706.4, y: 3163.3 }` had an ~117px horizontal error that caused lines to cut through market stalls.
  - Calibrated `NODE_TO_SVG_OFFSET` to exact mathematical center `{ x: 7823.47, y: 3174.0 }` (0.0000 coordinate diff across all SVG walkway nodes). Route line now runs dead-center along physical walking aisles (e.g. for Perillo's Eatery 1, exactly down the 63-unit aisle between slot_ea_1 and id_193).
  - Implemented progressive SVG stroke animation in `mapRenderer.drawRoute()` using `stroke-dasharray` / `stroke-dashoffset` transition over 1.2s with `cubic-bezier(0.2, 0, 0.2, 1)`, honoring `prefers-reduced-motion`.
  - Hardened tap-away dismissal in `uiController.js` so clicks on map controls (zoom in, zoom out, recenter) or header elements no longer accidentally dismiss active routes or open detail cards.
  - Verified: programmatic check on all 112 nodes passed with 0.0000 diff; `npm run build` clean (2.79s).

- Implemented Impeccable UX Wayfinding Refinement (Two-State Navigation Sheet):
  - Replaced all-or-nothing navigation modal with a responsive two-state model: `#nav-minimized-bar` (compact ~60px guidance bar) ↔ `#nav-expanded-content` (full turn-by-turn sheet).
  - Preserved active route polyline on map when minimizing or tapping outside the panel, enabling full mobile map inspection while navigating.
  - Added header minimize button, minimized bar expand button ("Steps ▲"), and explicit exit route buttons ("✕").
  - Dynamically lifted map controls (`+`, `−`, Recenter) above the minimized guidance bar when minimized, restoring zoom and recenter ergonomics during active navigation.
  - Evaluated with `impeccable detect` (0 defects) and validated via end-to-end browser subagent simulation.

- Connected Route Polyline Directly to Destination Stall Center:
  - Fixed route polyline stopping halfway at corridor snap nodes. Added `getStallCenter()` in `src/mapRenderer.js` extracting `{ x: bbox.x + bbox.width / 2, y: bbox.y + bbox.height / 2 }`.
  - Extended `drawRoute()` to append the destination stall center coordinate as the final point, connecting seamlessly from the aisle directly into the stall's center point.
  - Verified across `id_23` (ARLES ARMARIO STORE) and other stalls: route traces through walking aisles and finishes inside the stall rectangle.
